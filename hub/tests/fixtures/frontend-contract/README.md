Frontend contract fixtures generated by tests/test_frontend_contract_export.py.
Network for provider-status is mocked; do not treat these as live e2e captures.

Regenerate the committed copies (and pick up timestamp-format changes) with:

```
REGENERATE_FRONTEND_CONTRACT=1 pytest hub/tests/test_frontend_contract_export.py -q
```
